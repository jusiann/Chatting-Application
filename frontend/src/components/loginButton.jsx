import "../style/loginButton.css";

const LoginButton = () => {
    return (
        <button className="loginButton">
            <span>Giriş yap</span>
        </button>
    );
};

export default LoginButton;